<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kasir">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.StrukPenjualanActivity"></activity>
        <activity android:name=".activity.PaymentCash2Activity" />
        <activity android:name=".activity.PaymentCashActivity" />
        <activity android:name=".activity.PenjualanActivity" />
        <activity android:name=".activity.TransaksiDetailActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".activity.TransaksiActivity" />
        <activity android:name=".activity.ScanBarcodeActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:label="Home Screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.FormMasterDataActivity" />
        <activity android:name=".activity.MasterDataActivity" />
        <activity android:name=".activity.BarcodeActivity" />
        <activity android:name=".Home" />
    </application>

</manifest>